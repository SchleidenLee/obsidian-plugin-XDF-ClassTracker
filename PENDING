# PENDING - feedback_cache.json 磁盘缓存

## 功能目标
引入磁盘缓存替代当前每次刷新都读取正文的方式，提升课节笔记数量增长后的性能。

## 核心设计

### 缓存文件
- **路径**: 插件目录下的 `feedback_cache.json`
- **格式**:
  ```json
  {
    "path/to/note.md": {
      "need_send_feedback": true,
      "feedbackTaskDone": false,
      "last_updated": "2026-07-08T10:00:00+08:00"
    }
  }
  ```
- **初始构建**: 遍历所有 md 文件 → 过滤 `need_send_feedback: true` → 读取正文提取 checkbox 状态 → 写入缓存（首次略耗时）
- **增量更新**: 监听 `metadataCache.on('changed')` 事件，只处理变更的文件

### 触发条件
当前课节笔记数量较少（每天 1-6 篇），全量扫描性能无瓶颈。**待笔记数量明显增长（如 500+ 篇）且切换日期出现卡顿后再启用。**

## 涉及文件
- `main.ts` — 添加 `loadFeedbackCache` / `saveFeedbackCache`、修改 `extractFeedbackInfo` 优先读缓存
- `PENDING` — 本文档
